import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iglesias.mundosano',
  appName: 'Mundo_Sano',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
